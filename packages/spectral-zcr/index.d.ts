/** Zero-crossing rate — fraction of consecutive samples whose sign differs (librosa zero_crossing_rate class). */
/** Signal-domain (time-domain, not spectral): pass raw PCM directly. No options. */
export default function zcr(data: Float32Array | Float64Array): number
